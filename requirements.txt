# Garak Studio backend
# NOTE: `garak` must be installed in the same interpreter for introspection to
# work. It is intentionally NOT pinned here so you control its version:
#   pip install garak
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
aiosqlite>=0.20
cryptography>=42.0
python-multipart>=0.0.9
# Optional (Postgres): asyncpg>=0.29
# Optional (scheduling/queue): apscheduler>=3.10  redis>=5.0  rq>=1.16
